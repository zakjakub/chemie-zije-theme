{% set logo = site.theme.link ~ '/images/logo/prf_katedra-anorganicke-chemie_cz_barva.svg' %}

<header id="header" class="container website-header mt-0 mt-lg-2 mb-3">
    <div class="row justify-content-evenly">
        {# Logo and (hidden) name/title of website. #}
        <h1 class="d-flex align-items-stretch align-content-stretch col-12 col-md-3 order-1 order-md-0 fs-6 fs-lg-5 py-1 py-lg-2"
            style="min-height: 3em;">
            <a href="{{ site.url }}"
               rel="home"
               style="background-image: url({{ logo }}); background-position-x: left; background-position-y: center;">
                <span class="d-none">{{ site.name }}</span>
            </a>
        </h1>
        <nav class="col-12 col-md-9 order-0 order-md-1 primary-menu text-uppercase" role="navigation">
            {% include "navigations/menu/menu.html.twig" with {'items': menu.primary.get_items, is_header: true} %}
        </nav>
    </div>
    <form action="/" method="get" class="row collapse  {{ search|default ? 'show' : '' }}" id="searchCollapse">
        <div class="col-12 input-group my-2 px-0">
            <label for="search-input" class="btn btn-outline-primary" id="basic-addon1">🔎</label>
            <input type="search"
                   value="{{ search|default }}"
                   required
                   id="search-input"
                   name="s"
                   class="form-control"
                   placeholder="Hledané slovo"
                   aria-label="Vyhledávání"
                   aria-describedby="basic-addon1">
            <button class="btn btn-outline-primary" type="submit">
                Hledat
            </button>
        </div>
    </form>
</header>

