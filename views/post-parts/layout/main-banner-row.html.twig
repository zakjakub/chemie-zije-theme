{% set imageSrc = image.src|default(site.theme.link ~ '/images/photos/foto.png') %}

<header class="row page-banner bg-primary text-white mb-3 border border-primary border-2">
    <div class="col-12 col-xl-5 col-lg-6 image-box m-0 p-0"> <!--style="background-image: url({{ imageSrc }});"-->
        <img alt="CHEMIE ŽIJE!" class="img-fluid" src="{{ imageSrc }}">
    </div>
    <div class="col-12 col-xl-7 col-lg-6 title-box d-flex flex-column justify-content-center text-center">
        <h1 class="p-5 text-capitalize {{ isHome ? ' single-heading ' : ' pb-2 ' }}">
            {{ site.name }}
        </h1>
        <h2 class="p-5 pt-2 {{ isHome ? ' d-none ' : '' }}">
            {{ post.title }}
        </h2>
    </div>
</header>
