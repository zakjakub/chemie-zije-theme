{% if items|default and items|length > 0 %}
    <ul itemscope
        itemtype="http://schema.org/SiteNavigationElement"
        class="menu hover-bold"
        style="font-size: {{ fontSize ? fontSize : '1em' }}">
        {% for item in items %}
            {% include 'post-parts/layout/menu-item.html.twig' %}
        {% endfor %}
    </ul>
{% endif %}
