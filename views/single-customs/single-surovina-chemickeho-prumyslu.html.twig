{% extends "post-types/page.html.twig" %}

{% block article_signpost %}
{% endblock %}

{% block article_content %}
        <section class="row my-5">
            <div class="col-12">
                <h3>
                    {{- post.title -}}
                    {{- post.meta('_alternative_names') ? ' (' ~ post.meta('_alternative_names') ~ ')'  : '' -}}
                </h3>
                <div>
                    {{ post.content }}
                </div>
            </div>
        </section>
{% endblock %}
